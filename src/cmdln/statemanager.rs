// StateManager
// The component provides the functionality to manage the State 
// of a deployed rest api integration workload. It receives the parsed configuration file from the
// ArgParser constructs the structure of the workload and interacts with the specific resolver
// modules to enact the workloads on the platform.


